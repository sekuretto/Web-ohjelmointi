const A = [-6, -91, 1011, -100, 84, -22, 0, 1, 473];

/*function solution(A) {
  let pienet = A.filter(A => A < 9);
  console.log(pienet);
  let tulos = Math.max.apply(0, pienet);
  return tulos;
};*/



let ranks = [3,4,3,0,2,2,3,0,0];

/*function solution(ranks) {
  let reportingSoldiers = 0;
  ranks.forEach(function (rank) {
    let superior = rank + 1;
    let report = ranks.includes(superior);
    if (report = true) {
      reportingSoldiers++;
    };
  });
  return reportingSoldiers;
};*/

let S = "CBACD";
//CABCD
//ACBDACBD

function solution(S) {
  let arr = S.split("");
  for (i=0, i <= arr.length, i++) {
    if (i = "A") {

    }
  }
  arr.forEach(function(i) {
    let i = i.indexOf(i);
    let j = i.indexOF(i+1);
    if (i = )
  })
  return arr;
}

console.log(solution(S));

 

  //let tulos = Math.max.apply(0, pienet);